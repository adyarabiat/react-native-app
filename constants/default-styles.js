// This is another way of getting the fonts
// the first way is in the TitleText in the components

import { StyleSheet } from "react-native";

export default StyleSheet.create({
   bodyText: {
      fontFamily: "open-sans",
   },
   title: {
      fontFamily: "open-sans-bold",
      fontSize: 18,
   },
});
